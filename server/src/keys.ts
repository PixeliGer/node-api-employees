// Database connection configuration
export default {
    
    database: {
        host: 'localhost',      // Defuault host
        user: 'root',           // Using default db user (change to your local user if necessary)
        password: 'toor',       // Using default db pass (change to your local pass if necessary)
        database: 'employees'   // Using database employees
    }

}